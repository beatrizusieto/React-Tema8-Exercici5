import styled from 'styled-components';
import background from './images/reir.jpg';

const BackgroundStyled = styled.div`
background-image: url(${background});
background-size: cover; 
height: 660px
`;
export default BackgroundStyled